import cs1.Keyboard;

public class Main
{
	public static void main(String[] args)
	{
		System.out.print("Hours : ");
		int hours = Keyboard.readInt();
		System.out.print("Wage : ");
		int wage = Keyboard.readInt();
		int pay = 0;
		if(hours > 60)
		{
		pay = 40 * wage * );
		pay = 20 * (wage * 2);
		int sixtyhours = hours - 60;
		}
		if(hours > 40)
		{
		int fortyhours = hours - 40;
		}
	}
}
